import ProductList from "./ProductList";

const Content = () => {
    return (
        <div className="center border" style={{flex: 1}}>
            <ProductList />
        </div>
    );
};

export default Content;